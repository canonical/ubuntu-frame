#!/bin/sh
set -eu
display_file="$SNAP_DATA/frame.display"
if [ -z "$(snapctl get display)" ] && [ -e "${display_file}" ]  && ! grep "DO NOT EDIT THIS FILE BY HAND"  "${display_file}"
then
  snapctl set display="$(cat "${display_file}")"
fi
